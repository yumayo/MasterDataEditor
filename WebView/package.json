{
  "name": "webview",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run generate:view-plugin-types && vite build",
    "generate:view-plugin-types": "tsc -p tsconfig.view-plugin-api.json",
    "preview": "vite preview",
    "stop-preview": "kill $(lsof -t -i:4173) 2>/dev/null || true"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vite": "^7.2.2"
  }
}
