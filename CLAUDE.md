# 概要

マスターデータの入力支援ツールを開発します。

言語はバックエンドがC#でフロントエンドにTypeScriptを選択します。

フロントエンドはフレームワークを使用しない「Vanilla JS」です。

## 哲学
「フレームワークは思考の補助輪であり、本質的な設計力はVanillaで書くときに試される。」
データグリッドの真価は"見た目"ではなく"データの関係性をユーザーが直感的に操作できるか"にある。
TDDは品質保証ではなく設計手法であり、テストを先に書くことでAPIの使いやすさが自然と磨かれる。

## ゴール
ゲーム開発や業務システムなどで使われるマスターデータ（アイテムテーブル、敵テーブル等）を、非エンジニアでも安全かつ効率的に編集できるツールを作ること。
特に「外部キーで参照しているIDを手打ちする苦痛」を解消し、JOINされた人間可読な状態で編集→保存時に正規化されたCSVに戻す、というワークフローを実現する。

# AI あなたに対する説明

- あなたは安全なコンテナ上で実行されています。
- ネットワークアクセスを遮断されています。
- そのため、npm run build や dotnet build 等のパッケージをダウンロードする系のものはすべて実行できません。
- ネット検索もできないので、回答は記憶している限りで問題ありません。
- 知識量が不足している場合は、実行を止めてください。
- 日本語で回答してください。

# 実装フロー

`tddスキル` 参照。

# ルール

## 責務を最も大切にせよ

実装を開始するときは、一回待ってください。
トークンやコンテキスト量を気にすることなく「本当にここに実装を置いていいのか？」自分を問いただしてください。
必要最小限の修正よりも、大胆に修正して現状最適なコードを目指してください。

## 基本

- 実装ではTDDサイクル設計テストファースト戦略を採用してください。
- 過度に改行されているところは参考にしないでください。行を名一杯使ってください。
- ファイルの文字コードは UTF-8 を使用してください。
- 改行コードは CRLF を使用してください。
- 実装フローに基づき修正してください。
- すべての動作でUndoとRedoに対応してください。Commandパターンで実装されています。

# コーディングガイドライン

前提として、世の中にあるありとあらゆるプログラミング原則を満たすことを何より重視してください。
ロジカルシンキングとクリティカルシンキングを駆使して原理原則をすべて満たす解を導き出してください。

## プログラミング原則

- デメテルの法則を守ってください。
- setterは禁止
- public element HTMLElement は絶対に作らない
- デフォルト引数は使わない
- フォールバックをしない
- anyを使用しない
- undefinedを使用しない
- nullを使用しない
- 疎結合ではなく **密結合** にしてください
- Aクラスの機能をBクラスで使いたいが、AクラスもBクラスの機能を使いたいときのような相互参照が発生するケースでも **密結合を重要視** してください。コールバックではなく、相互参照で良いので関数を直接呼び出してください。相互参照クラスの作り方は **typescriptスキル** を見てください。
- コードは読みやすく、明確に記述してください。
- コメントは日本語で記述し、コードの意図を補足してください。
- 一貫したインデントとスタイルを守り、レビューを円滑に進めましょう。
- private関数は複数箇所で再利用する場合のみ導入する。1回しか使われない場合はインライン展開し、責務の分散を避ける。
- 非同期の関数はサフィックスにAsyncを付けてください。
- 共通処理が書ける場合は、共通処理でまとめること。決して、コピペを多用しないこと。
