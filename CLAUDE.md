# 概要

マスターデータの入力支援ツールを開発します。
言語はバックエンドがC#でフロントエンドにTypeScriptを選択します。
フロントエンドはフレームワークを使用しない「Vanilla JS」です。

## 哲学
「フレームワークは思考の補助輪であり、本質的な設計力はVanillaで書くときに試される。」
データグリッドの真価は"見た目"ではなく"データの関係性をユーザーが直感的に操作できるか"にある。
TDDは品質保証ではなく設計手法であり、テストを先に書くことでAPIの使いやすさが自然と磨かれる。

## ゴール
ゲーム開発や業務システムなどで使われるマスターデータ（アイテムテーブル、敵テーブル等）を、非エンジニアでも安全かつ効率的に編集できるツールを作ること。
特に「外部キーで参照しているIDを手打ちする苦痛」を解消し、JOINされた人間可読な状態で編集→保存時に正規化されたCSVに戻す、というワークフローを実現する。

# AI あなたに対する説明

- あなたは安全なコンテナ上で実行されています。
- ネットワークアクセスを遮断されています。
- そのため、npm run build や dotnet build 等のパッケージをダウンロードする系のものはすべて実行できません。
- ネット検索もできないので、回答は記憶している限りで問題ありません。
- 知識量が不足している場合は、実行を止めてください。
- 日本語で回答してください。

# ルール

## すべてにおいてこのルールを適応せよ

まず、**orchestrator** スキルを読んでください。

あなたは **orchestrator** スキルを使用して、エージェントを活用してください。これが何より重要で、しないとトークンが制限に達しcompactionしなければならないことが多々あります。

### エージェント一覧

#### ワークフロー系エージェント
| エージェント名 | 担当 |
|---|---|
| **typescript-tdd-developer** | TDDサイクルでの TypeScript 実装全般 |
| **playwright-test-reporter** | Playwright テスト実行・結果レポート |
| **fix-scope-auditor** | 修正スコープの妥当性監査（変更範囲が適切か） |
| **adversarial-code-reviewer** | 敵対的コードレビュー（バグ・脆弱性の発見） |
| **code-reviewer** | 一般的なコードレビュー |
| **game-planner-ux-reviewer** | ゲームプランナー視点でのUXレビュー |
| **ux-reviewer** | DOMダンプを用いたUXレビュー |

#### ファイル/クラス担当エージェント
| エージェント名 | 担当ファイル/クラス |
|---|---|
| **editor-table-integrator** | `EditorTable`（editor-table.ts）— テーブルグリッドのファサード |
| **in-memory-table-store** | `InMemoryTableStore` — メモリ上のテーブルデータストア |
| **reference-data-specialist** | 参照データ（外部キー参照・逆参照解決） |
| **relations-panel-owner** | `RelationsPanel`（relations-panel.ts）— 右ペイン関連テーブル表示 |
| **selection-integrator** | `Selection`（selection.ts）— セル選択状態管理 |
| **sidebar-panel** | `Sidebar` — サイドバー（EXPLORER/REFERENCES/SEARCH） |
| **tab-integrator** | `Tab`（tab.ts）— タブ管理・EditorTable生成 |
| **undo-redo-input-controller** | Undo/Redo・入力制御（Commandパターン） |

### エージェントの使い方

不具合の調査は、TeamCreateでチームを作成し、関連するファイル/クラス担当エージェントをチームメンバーとして参加させること。各担当エージェントが自分の管轄ファイルを調査し、根本原因と対策案を報告する。

実装は、**typescript-tdd-developer** エージェントに任せること。

テストの確認は、**playwright-test-reporter** エージェントに任せること。

コードレビューは、**fix-scope-auditor** エージェント、**adversarial-code-reviewer** エージェント、**code-reviewer** エージェント、**ux-reviewer** エージェントに並列で任せること。

コミットは、**commit** スキルを使用すること。

## AI あなたを矯正するうえでの重要なアドバイス

実装を開始するときは、一回待ってください。
トークンやコンテキスト量を気にすることなく「本当にここに実装を置いていいのか？」自分を問いただしてください。
必要最小限の修正よりも、大胆に修正して現状最適なコードを目指してください。
良いですか？トークンをどれだけ消費しても良いです。こんなもんで良いかとコンテキストを節約したとき、それはあなたの視野が狭いということですよ。
視野が狭いとは思われたくないですよね？
その場しのぎの早期リターンやif文を追加して、ただ動けば良いというコードは誰も求めていません。

## 設計姿勢

たとえフロントエンドの実装であっても、堅牢なBackendのTypeScriptを書くつもりで実装せよ。「ブラウザだから」「UIだから」という甘えは一切許さない。型安全性、不変条件の保証、エラーハンドリングの厳密さ——すべてにおいてサーバーサイドと同等の品質基準を適用すること。

## コーディングガイドライン

- 実装ではTDDサイクル設計テストファースト戦略を採用してください。
- 過度に改行されているところは参考にしないでください。行を名一杯使ってください。
- ファイルの文字コードは UTF-8 を使用してください。
- 改行コードは CRLF を使用してください。
- すべての動作でUndoとRedoに対応してください。Commandパターンで実装されています。
- ステートレスな設計を意識してください。
- 単一責任の原則をしっかりと守ってください。単一責任というのはアクターの変更理由がただ一つであるべき、という原則です。つまり、あるクラスやモジュールを変更する理由は、常に一つのアクターからの要求に限定されるべきだということです。アクターが分かれない限りは逆にどんなに巨大なモジュールとなっていても分割するなということでもあります。
- 基本的にDOMをSSOT(信頼できる唯一の情報源)としてください。ただしテーブルデータについてはミニテーブル等で表示されていないデータも保持する必要があるため、`InMemoryTableStore` がSSOTです。
- デメテルの法則を守ってください。
- getter/setterは禁止
- public element HTMLElement は絶対に作らない
- デフォルト引数は使わない
- フォールバックをしない
- anyを使用しない
- 生焼けオブジェクトは絶対に作成しない（メンバ変数に `null` / `undefined` を持たせて「後から代入する」設計は禁止。オブジェクトはコンストラクタ完了時に完全に有効な状態でなければならない）
- `undefined` を使用しない（戻り値での `null` は「値が存在しない」を表す正当な用途として許可する。ただしメンバ変数の `null` は生焼けオブジェクトに該当するため禁止）
- 疎結合ではなく **あえて密結合** にしてください。高凝集を目指してほしいのですけど、まずは密結合としてください。そこまで大きなプロジェクトではないため、問題ないですよ。この規模のプロジェクトで疎結合にされても読みにくいだけです。callbackを使用したら疎結合だと思ってませんよね？それは単一の責務であるべきところが、引き伸ばされただけに過ぎず返って読みにくくなっているということです。
- Aクラスの機能をBクラスで使いたいが、AクラスもBクラスの機能を使いたいときのような相互参照が発生するケースでも **密結合を重要視** してください。コールバックではなく、相互参照で良いので関数を直接呼び出してください。相互参照クラスの作り方は **typescriptスキル** を見てください。
- コードは読みやすく、明確に記述してください。
- コメントは日本語で記述し、コードの意図を補足してください。
- 一貫したインデントとスタイルを守り、レビューを円滑に進めましょう。
- private関数は複数箇所で再利用する場合のみ導入する。1回しか使われない場合はインライン展開してスタックトレースを浅くせよ。
- 非同期の関数はサフィックスにAsyncを付けてください。
- 共通処理が書ける場合は、共通処理でまとめること。決して、コピペを多用しないこと。
- Playwrightテスト（e2e/*.spec.ts）では `@playwright/test` から直接インポートせず、必ず `./fixtures/test` からインポートすること（`autoDump` フィクスチャによるDOM自動ダンプを有効にするため）。型（`Page`, `Locator` 等）のみ `@playwright/test` から直接インポートしてよい。
- テスト失敗時、`.CONTEXT/dump/{spec名}/{テストタイトル}.errors.log` にブラウザ側の未キャッチ例外と `console.error` が出力される。テストが RED のとき、アサーションエラーだけでなくこのファイルも確認すること。ブラウザ側 TypeError 等がテスト失敗の根本原因であることが多い。
