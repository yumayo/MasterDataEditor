# 問題

現在、スキーマで定義された列の型（int, string等）と一致しない値をセルに入力しても、バリデーションエラーとして扱われない。
例えば int 型の列に文字列を入力してもエラー表示されず、不正なデータがそのまま保存される可能性がある。

# 期待する動作

- セルの値がスキーマで定義された列の型と一致しない場合、バリデーションエラーとして視覚的に表示すること
- 例: int 型の列に「abc」を入力した場合、セルがエラー状態（赤枠・赤背景等）で表示される
