例えば、questテーブルのクエスト報酬グループIDの列を入力する人の立場だとして、そのグループIDの中身がわからないと入力しにくいと思います。
そこで、プルダウンメニューのIDにマウスオーバーすると右側(画面をはみ出るなら左側)にクイックビューでモーダルを表示してください。
そのモーダルにはRelationsパネルを表示して、そのグループIDがどのようなデータに紐づいているのかを表示してください。
