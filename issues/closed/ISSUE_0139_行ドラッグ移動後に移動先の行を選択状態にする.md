# ISSUE_0139 行ドラッグ移動後に移動先の行を選択状態にする

## 概要

行のドラッグ移動後、選択状態が移動元の行に残ったままになっている。
移動先の行が選択されるべき。

## 再現手順

1. 7行目を選択してドラッグで14行目に移動する
2. 移動後、移動元の7行目が選択状態のままになっている（**NG**）

## 期待動作

行の移動完了後、移動先（14行目に挿入された行）が選択状態になること。
